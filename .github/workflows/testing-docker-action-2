name: Testing docker action
on: [workflow_dispatch]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        uses: Sazzo100/hello-world-docker-action@v1
        with:
          who-to-greet: '@Sazzo100'
